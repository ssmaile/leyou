server:
  port: 10086
spring:
  application:
    name: leyou-register
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka
    fetch-registry: false  #不拉去服务
    register-with-eureka: false #不注册服务
#  server:
#    enable-self-preservation: false  #不自我保护
#    eviction-interval-timer-in-ms: 10000  #清理无效链接